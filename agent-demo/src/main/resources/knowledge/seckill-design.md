# 秒杀库存扣减设计要点

秒杀库存接口需要同时考虑入口限流、Redis 预扣减、MySQL 最终一致性和幂等流水。

推荐链路：

1. Controller 校验商品、用户、请求幂等键。
2. Redis 使用 Lua 脚本原子判断库存并预扣减。
3. 预扣成功后写入订单或扣减流水，流水表需要唯一业务键。
4. MySQL 使用乐观锁或库存扣减条件 `stock >= quantity` 防止超卖。
5. 失败时通过补偿任务恢复 Redis 库存或标记流水失败。

接口返回应该区分排队中、扣减成功、库存不足、重复请求、系统繁忙。
